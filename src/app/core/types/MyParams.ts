export type MyParams = {
  database?: string // Un objeto simple
  item?: object // Un objeto simple
  items?: Array<any> // Array de objetos de una clase
  path?: string // Ruta de angular
  payload?: any
}
